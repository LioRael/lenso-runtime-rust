// Internal owner-release evidence; deliberately absent from package exports.
// A bounded settlement receipt stays sticky while native release is observed.
export const cleanupRelease = new WeakMap();
