[package]
name = "lenso-workers-g2-request-body-tests"
version = "0.0.0"
edition = "2024"
publish = false

# Test the exact Host decoder on native Rust without the Wasm-only Driver and
# unpublished Web business fixtures. The full G2 Wasm check remains required.
[workspace]

[dependencies]
base64 = "=0.22.1"
bytes = "=1.12.1"
http = "=1.5.0"
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
